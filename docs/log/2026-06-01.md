# 2026-06-01 — STATUS reconciliation (git vs prose drift)

**Context**: `/catchup all` briefed this project from a stale STATUS.md and got it wrong — reported "next: Stage 1, salvage the export-tool" when in fact Stages 1–8 had already shipped. Root-caused the drift and reconciled STATUS against git. No project code written this session.

**Done**:
- **Root-caused the bad catch-up.** The 2026-05-28 evening session recorded the Quartz→thin-renderer *decision* in STATUS, then *executed* 7 commits of it (`c9ffb1e`→`418ae0a`) in the same sitting without re-closing out. STATUS froze at "about to start"; git raced to "mostly done." `/catchup all` (cross-project mode) reads only session-drafts — which were empty — so it fell back to the stale prose.
- **Verified true git state**: thin Python renderer live, real 29-page ITSAI corpus deployed, hub pages + collapsible sidebar redesigned, CI building with the renderer, last GH Pages deploy green at 2026-05-28 22:15. Live site HTTP 200, zero "Quartz".
- **Hardened both lifecycle skills** so this can't recur (edits to `~/.claude/skills/`):
  - `catchup` — git is now the ground truth in every mode; reads STATUS `_Last updated:_`, runs `git log --since`, and prints a staleness warning + reconstructs from commit subjects when git is ahead. Cross-project mode no longer treats empty session-drafts as "no work."
  - `end` — Step 2 now reconciles git against STATUS *before* routing, so a session that executed past its own plan can't close out with a stale "What's next."
- **Reconciled `docs/STATUS.md`** to git reality: added a reconciliation note, rewrote State / What's working / What's next, and updated the open question about the 300-line ceiling (answered: breached, now 517).

**Discoveries / gotchas**:
- `tools/render.py` is **517 lines, past ADR-0002's 300 hard ceiling** — it grew during the two "impeccable" hub/sidebar redesign commits. The MkDocs escape hatch was never triggered; this is feature growth, not an integration failure. Needs an explicit decision (re-baseline via ADR note, or refactor).
- Uncommitted in the tree: `site/content/data-ai/guides/approved-tools-comparison.md`, 3 `summer-intern-2026/` test pages, stray `claude/claude-faq.md` + `test-resume-tailor-machine-brain*` seed/test files, plus design screenshots and a `.tmp-chrome-360/` scratch dir. Not lost, but unrouted — needs per-file triage.
- **Process lesson**: `git status` clean ≠ STATUS current. Committing to GitHub does not update the prose. Close out (`/end` or at least a STATUS "What's next" tweak) whenever commits outrun what STATUS describes — the clock isn't the trigger, shipped work is.

**Decisions**: none (no new ADR). The 300-line-ceiling reckoning is flagged as an open question for a future ADR, not decided here.

**Next**:
- Stage 9 — Aaron's research memo (the actual internship deliverable).
- Triage the uncommitted content (commit real pages, gitignore/delete test scaffolding).
- Decide the renderer line-count question (ADR re-baseline vs refactor).

---

## Afternoon — vision reframe + public-only access decision

**Context**: Pinned down what the KB site actually is (vs. how its docs/copy described it) and made the access model decision.

**Done**:
- **Reframed the project around its real vision**: a Syracuse-owned, markdown-native KB the university hosts and controls end-to-end — *migrating off* Confluence, not syncing to it. Confirmed the site falsely claimed "Auto-synced from Confluence ● / Last sync: today" (hardcoded) — none of which was true (no scheduled sync; export is a manual one-time seed).
- **Removed Confluence entirely from the public site** (`landing.html.jinja`, `docpage.html.jinja`): killed the false sync/"source of truth" copy, the Confluence edit/source links, and a footer that leaked the `ITSAI` space key. Reframed to "SU-hosted Markdown / controls end to end." Verified the rendered chrome is Confluence-free.
- **Reframed the context/instruction files** to the vision (`CLAUDE.md`, `README.md`, `site/CONTEXT.md` [was 100% stale Quartz], `export-tool/CONTEXT.md`, STATUS scope).
- **Fixed a build-breaking bug** in `tools/render.py` `related_for()` — PyYAML parses unquoted `last_modified` as `date` vs quoted as `str`, breaking the sort tiebreaker. Coerced the key to `str`. Build now green with all content (36 pages). The crash was triggered by the uncommitted test pages.
- **Discovered the access gap**: the export tool has no real visibility check — it would publish any restricted page not caught by the name filter. The ITSAI space *does* contain restricted pages.
- Wrote the execution plan: [`../plan-public-only-access.md`](../plan-public-only-access.md).

**Decisions**: [ADR-0003](decisions/0003-public-only-access-classification.md) — public-only publication via an export-time access classifier (closes the access gap ADR-0002 left implicit).

**Next**: execute the public-only classifier plan; then commit + push (nothing today is live until pushed); resolve the uncommitted test-page triage; Stage 9 (Aaron's memo).
