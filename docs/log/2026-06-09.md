# 2026-06-09 — v1 fail-closed gate shipped + proven; curator backlog cleared

**Context**: Start-of-session `/catchup` flagged the v1 close-out (push + branch protection +
blocked-PR test) and a curator backlog. Tackled both: closed out v1 end-to-end and applied the
top curator proposals (2 CRITICAL memory updates + 4 backlogged CC KB technique notes).

**Done**:
- **Reconciled + pushed `main`.** Origin had diverged — the web-UI live-test "Hermes Agent" page
  (PR #1, `afa5ad2`) was ahead of local. Merged it in (`fa281a6`, conflict-free, additive page)
  and pushed all 11 local commits. The push first failed because the `gh` token lacked `workflow`
  scope (it carries `validate-content.yaml`); resolved by `gh auth refresh -s workflow`.
- **Branch protection live on `main`** (rules #1 + #2 of `branch-protection.md`, via `gh api`):
  require-a-PR (0 approvals, `require_code_owner_reviews: false` per [ADR-0004](../decisions/0004-human-gate-via-restricted-merge-access.md)),
  required `validate` status check, `enforce_admins: true`, force-push/deletion blocked. **Rule #3
  (restrict _who_ can merge) deferred** (`restrictions: null`) pending the maintainer set from Aaron.
- **Blocked-PR test passed (task 1.8) — the fail-closed proof.** Opened PR #2 with a deliberately
  invalid native page (missing `visibility`). The `validate` check **failed** (`E_BAD_VISIBILITY`)
  and GitHub reported the merge **BLOCKED**. Closed unmerged; branch deleted. An invalid content PR
  provably cannot merge — VISION-P4 demonstrated, not assumed.
- **Curator: 2 CRITICAL stale memories updated** — `project_su_kb_site` (CONDITIONAL-GO → SHIPPED)
  and `project_veritas` (v3/ChatGPT-round → front-end-first build, v4), plus both `MEMORY.md` lines.
- **Curator: 4 backlogged CC KB technique notes** drafted via a multi-agent workflow (draft →
  adversarial verify), all PASS: `multi-llm-vision-critique-loop`, `front-end-first-product-discovery`,
  `adversarial-ia-review-before-build`, `fold-blockers-into-scope` (the last grounded in this repo's
  ADR-0004). `index.md` reconciled; now-valid cross-links wired. (CC KB is local-only, not committed here.)

**Decisions**: no new ADRs. Two enforcement judgment calls on the branch-protection settings:
`enforce_admins: true` (so the fail-closed proof is genuine, not admin-bypassable) and `strict: false`
(no "update branch before merge" — keep author friction low).

**Next**:
- **Confirm the KB maintainer set with Aaron (Phase 0.2)**, then set `restrictions` to add rule #3
  (restrict merge to maintainers). The machine gate is already fail-closed without it.
- **Phase 4 fast-follows** (not v1 blockers): `visibility: public` backfill over the 29 legacy pages +
  export tool; optional renderer fix for the `> [!note]-` callout collapse.
- Optional CC KB enrichment: each new note flags where Julian's lived rationale would deepen it.
