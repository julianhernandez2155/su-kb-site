# 2026-05-28 — Project scaffold + architecture decisions

**Context**: First session for the new `su-kb-site` prototype, which replaces the prior `su-kb-pipeline` RAG/MCP architecture with a static GitHub Pages site + Claude skill model (Aaron's 2026-05-20 direction).

**Done**:
- Reviewed prior `su-kb-pipeline` codebase end-to-end; identified ~30–40% salvageable (Confluence-export half) and ~60% cut (chat/web/access)
- Ran deep-research skill on two questions: (1) LLM `WebFetch` optimization for markdown on GH Pages, (2) wikilinks + Obsidian frontmatter survival on GH Pages. Full report saved at `deep-research/reports/2026-05-28-llm-fetch-and-obsidian-ghpages.md`
- Key research finding: Claude Code's `WebFetch` already sends `Accept: text/markdown, */*` automatically (per @bcherny 2025-11-12). Raw `.md` beats rendered HTML by ~10× in token efficiency. Schema.org markup has weak empirical evidence — skip for MVP
- Invoked `/folder-architect` for the new prototype's structure; grounded recommendations in CC KB notes ([[folder-architecture-mistakes]], [[three-layer-context-architecture]], [[code-project-workspace-starter]], [[handoff-ready-workspace]], etc.)
- Wrote and got approval for the full plan at `C:\Users\julia\.claude\plans\fully-plan-plan-out-tranquil-sunbeam.md`
- Created the new folder tree at `SU_AI_Intern/prototypes/su-kb-site/` with root `CLAUDE.md`, `README.md`, `.gitignore`, `.env.example`, per-workspace `CONTEXT.md` stubs, and `docs/` scaffolding via the decision-log skill convention

**Decisions**:
- [ADR-0001](decisions/0001-quartz-v4-as-ssg.md) — Pin Quartz v4 as the SSG (v5 plugin ecosystem too immature; MkDocs-Material is the documented fallback if styling integration fights for >2 days during Stage 2)
- URL hierarchy: collapse Confluence wrappers; `/data-ai/` namespace replaces the redundant `knowledge-bases > Artificial Intelligence (AI) > AI @ Syracuse University > AI` chain. AI team's real taxonomy (Claude/, Copilot/, Gemini/, Clementine Platform/, AI - General Information/) sits at the top of the department
- Repo strategy: one repo for the prototype (`julianhernandez2155/su-kb-site`), split later when shipping

**Next**:
- `git init` + first commit
- `gh repo create julianhernandez2155/su-kb-site --private`
- Stage 2 — Quartz spike inside `_spike/` (`npx quartz create`, drop Codex's clementine assets into `quartz/styles/` + `quartz/static/`, adapt layouts, push 3 seed pages, verify GH Pages publishes)
