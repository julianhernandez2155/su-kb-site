# 2026-06-04 — RPI brainstorm: faculty page-authoring front-end

**Context**: STATUS's standing "What's next" was the authoring workflow (how new markdown
pages get added now that the site is hand-authored-markdown-as-source-of-truth). Ran
`/rpi:brainstorm` to shape that into a scoped REQUEST.

**Done**:
- Wrote `rpi/faculty-page-authoring/REQUEST.md` (RPI Step 1). Feature = a **Claude-native AI
  drafting front-end** for non-technical authors (esp. faculty): "here are some docs" → clean
  GFM + valid 8-field frontmatter (native-page convention: omit `page_id`/`source_url`) →
  lands via the GitHub web UI.
- Shaped three contested branches (grill-me style): **author model** = true faculty
  self-serve; **faculty surface** = free path only (draft in SU Claude Enterprise, submit via
  GitHub web UI — no paid API, no hosted backend); **publish gate** = self-serve authoring but
  the **human CODEOWNERS review gate is retained** (reconciles with VISION principle 4 — fails
  closed; "self-serve" = no operator needed, not zero review).
- **Discovery:** intern Robert already designed a six-file contribution-guardrail set
  (`new_page.py` scaffolder, `check_frontmatter.py` validator, `validate-content.yaml` CI,
  `CODEOWNERS` reviewer, PR template, research doc) covering placement + validation +
  publish-governance. This feature scopes to sit **on top of** that back-end, not rebuild it.
- **GATE 0 flagged:** Robert's files are **not merged** — verified `tools/new_page.py` is
  absent from the repo; they currently live only in a Downloads doc. Research must confirm
  their real state before building on them.

**Decisions**: none recorded as ADRs (pre-research scoping; ADR comes on `/rpi:research` GO).

**Next**: `/rpi:research faculty-page-authoring` (GO/NO-GO gate) — GATE 0 first.

---

## RPI research — faculty-page-authoring (GO/NO-GO)

**Context**: Ran `/rpi:research` on the REQUEST above. GATE 0 (is Robert's guardrail
back-end actually in the repo?) was the make-or-break check, so I verified it directly in
the repo rather than trusting the brainstorm's assumption.

**Verdict: CONDITIONAL GO** (confidence High). Report at
[`rpi/faculty-page-authoring/research/RESEARCH.md`](../../rpi/faculty-page-authoring/research/RESEARCH.md).

**Done / verified against the live repo:**
- **GATE 0 FAILED.** All six of Robert's guardrail files are **absent**: `tools/new_page.py`,
  `tools/check_frontmatter.py`, `.github/workflows/validate-content.yaml`, real `CODEOWNERS`,
  real PR template (only `_spike/` Quartz leftovers). **`skill/SKILL.md` is also absent** —
  `skill/` holds only `CONTEXT.md`. The back-end this feature scopes to sit "on top of" does
  not exist yet.
- **Schema fork discovered.** The only frontmatter validator in the repo,
  `export-tool/src/su_kb_export/frontmatter.py`, lists `page_id` + `source_url` in
  `REQUIRED_FIELDS` — the exact two fields the native-page convention says to omit. A native
  page would fail it. Robert's unbuilt `check_frontmatter.py` must branch on page origin.
- **Resolved favorably (from code):** Open Q4 placement works without `new_page.py`
  (`render.py:142` `dept = meta.get("department") or parts[0]`); Open Q6 SKILL.md page-map
  sync is **moot** (`emit_llms_txt()` regenerates the index on every deploy — thin-skill/
  fat-index); Open Q5 department selection is easy (small legible `data-ai/` taxonomy).
- **Still open:** friction floor (GitHub-account/PR literacy — stakeholder call for Aaron) and
  the `visibility: public` field decision (Q7, for plan).

**Decisions**: CONDITIONAL GO recorded in the research report; no ADR written yet (offered at
`/end`, deferred). The build-but-gate rationale can be captured via `/decide` before `/rpi:plan`.

**Next**: clear the three conditions (guardrails land or fold into scope · reconcile
native/export schema · Aaron sign-off on friction floor + demand), then
`/rpi:plan faculty-page-authoring`.

---

## AI-retrieval surfaces + agent_site_bench (committed 7861382, captured retroactively 2026-06-08)

**Context**: This work landed on 2026-06-04 (commit `7861382`) but was never written into
STATUS or this log — surfaced during a `/catchup` git-vs-prose reconciliation on 2026-06-08
and recorded here so it stops being invisible.

**Done**:
- **Hardened the machine-readable retrieval surfaces** (generated-surface-only — no edits to
  `site/content/`, `_design/` styling, or layout). Expanded the generated `robots.txt`
  AI/search-crawler allowlist in `tools/kb_config.py` (OpenAI `OAI-SearchBot`/`ChatGPT-User`/
  `GPTBot`; Anthropic `Claude-User`/`Claude-SearchBot`/`ClaudeBot`/`anthropic-ai`; Perplexity;
  Google/`Google-Extended`; Apple) and added machine-surface emission to `tools/render.py`
  (+153 lines).
- **Built `tools/agent_site_bench/`** — a CLI benchmark harness comparing this site vs
  Shahaan's `su-kb-pages-demo` on agent/crawler/LLM usability: surface audit, no-paid
  `llms.txt`→`.md` retrieval probe (fetch count / bytes / status / expected-page hit rank),
  optional OpenRouter tool-calling agent loop, and a judge → `results.json` + `report.html`.
  Smoke tests included; generated results are gitignored.
- **Wrote `docs/AI_RETRIEVAL_OPTIMIZATION_REPORT_2026-06-03.md`** — before/after surface scan
  vs Shahaan's site. Key finding: Shahaan's machine surfaces (`llms.txt`, `llms-full.txt`,
  JSON-LD, homepage `.md` alternate) work at his project root but **404 at the `/itsai/` skill
  route**, making his configured entry point fragile; this site serves `robots.txt`/`sitemap`/
  `llms.txt` 200 at the configured route with 29/29 `.md` twins + `source_url` provenance.
  Borrow candidates: `llms-full.txt` companion + JSON-LD + homepage `.md` alternate.

**Decisions**: none as ADRs (surface tuning + a benchmark tool, not an architecture pivot).

**Next** (folds into the existing backlog): consider the borrow candidates (`llms-full.txt`,
JSON-LD, homepage `.md` alternate) and an `OPENROUTER_API_KEY` paid agent-loop run — both
**ask-before-spend** (OpenRouter is a paid API; per the API-spend rule).
